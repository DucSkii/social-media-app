import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
  avatar: {
    width: '30px',
    height: '30px',
  },
  link: {
    textDecoration: 'none',
  },
}))