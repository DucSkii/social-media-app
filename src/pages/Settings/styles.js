import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
  settings: {
    marginTop: '50px',
  },
  darkMode: {
    padding: '10px',
    display: 'flex',
    alignItems: 'center',
  },
}))